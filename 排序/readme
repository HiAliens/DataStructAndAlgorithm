一、
排序为：冒泡排序，选择排序，插入排序，时间复杂度为O（n），排序的核心操作为比较元素的大小
排序为稳定排序（及排序完，相同元素的前后顺序不变）：冒泡排序、插入排序
二、
合并排序：时间复杂度为O（nlogn），可以借助实现合理的合并函数从而达到稳定排序，思想是借助递归，先分组（每个元素为一组），后排序，最后合并每次的排序结果。
快速排序：时间复杂度O（n^2），不稳定排序，原地排序，借助递归，先排序（这里需要找到一个中间点，想见代码）后分组，
